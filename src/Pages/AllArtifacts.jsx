import React, { useEffect } from 'react';

const AllArtifacts = () => {
            useEffect(() => { document.title = "All Artifacts " }, [])
    
    return (
        <div>
            All 
        </div>
    );
};

export default AllArtifacts;